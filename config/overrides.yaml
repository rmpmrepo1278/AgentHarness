providers:
  llamacpp_small:
    type: llamacpp
    tier: local
    endpoint: http://localhost:8080/v1
    model_hint: "9B"
    enabled: true
  llamacpp_large:
    type: llamacpp
    tier: local
    endpoint: http://localhost:8081/v1
    model_hint: "35B"
    enabled: true
  groq:
    enabled: true
  google:
    enabled: true
  cerebras:
    enabled: true
  sambanova:
    enabled: true
  openrouter:
    enabled: false
  openai:
    enabled: false
  anthropic:
    enabled: false

routing:
  low: [llamacpp_small, llamacpp_large, groq, cerebras, sambanova]
  medium: [groq, cerebras, sambanova, llamacpp_large, google]
  high: [groq, cerebras, sambanova, llamacpp_large, google]
  critical: [groq, cerebras, sambanova, llamacpp_large, google]

policies: []
